import * as React from 'react';
import SignupForm from './SignupForm';

export interface FormikProps {}

export default function Formik(props: FormikProps) {
   return (
      <>
         <SignupForm />
      </>
   );
}
